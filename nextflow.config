// nextflow.config

// Configure the executor to use the 'local' executor with default resources
executor {
    name = 'local'
    // No specific resource configuration required for the 'local' executor
}
